import 'package:flutter/material.dart';

class AppColors {
  // ── National / Government palette ──────────────────────────────
  static const Color primary = Color(0xFF1A365D);      // Deep Navy Blue
  static const Color primaryDark = Color(0xFF0D2545);
  static const Color primaryLight = Color(0xFF2B4C7E);
  static const Color saffron = Color(0xFFEA580C);       // Ashoka Saffron
  static const Color secondary = Color(0xFF059669);      // Emerald Green (pass/success)
  static const Color error = Color(0xFFC62828);          // FAIL / error
  static const Color warning = Color(0xFFF57F17);
  static const Color info = Color(0xFF0284C7);

  // ── Surfaces ───────────────────────────────────────────────────
  static const Color surface = Color(0xFFF8FAFC);
  static const Color card = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE2E8F0);
  static const Color border = Color(0xFFE0E4E8);
  static const Color scaffoldDark = Color(0xFFF1F5F9);

  // ── Text ───────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF546E7A);
  static const Color textHint = Color(0xFF94A3B8);

  // ── Role-specific accents ──────────────────────────────────────
  static const Color vendorAccent = Color(0xFF2563EB);   // Bright Blue
  static const Color lmoAccent = Color(0xFF1A365D);      // Same as primary
  static const Color demoGold = Color(0xFFD97706);       // Demo-mode highlight
}
